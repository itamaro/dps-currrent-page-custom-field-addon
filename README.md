dps-currrent-page-custom-field-addon
====================================

Allow parsing "current" as the current page ID when using the display posts shortcode plugin to query custom fields using the meta_key and meta_value attributes.
